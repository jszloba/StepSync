/** @type {import('next').NextConfig} */
const nextConfig = {
      images: {
          domains: ["cloud.appwrite.io", "jetbrains.com"],
      },
    reactStrictMode: false,
}

module.exports = nextConfig
